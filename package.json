{
  "name": "io-utils",
  "version": "1.0.1",
  "description": "javaScript utils set",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run clean && npm run dist && npm run babel",
    "clean": "rimraf lib && rimraf dist",
    "babel": "babel src --out-dir lib",
    "babel2": "babel src --out-dir lib --ignore src/index.js",
    "dist": "webpack --config webpack.config.js --progress --profile --bail",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zifan",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sunioio/io-utils.git"
  },
  "keywords": [
    "javascript",
    "utils",
    "tools"
  ],
  "bugs": {
    "url": "https://github.com/sunioio/io-utils/issues"
  },
  "homepage": "https://github.com/sunioio/io-utils#readme",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-plugin-transform-runtime": "^6.22.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.22.0",
    "rimraf": "^2.6.2",
    "webpack": "^3.10.0"
  },
  "dependencies": {
    "babel-cli": "^6.26.0"
  }
}
